I will use the system message as a guide.

``python
last_user_message = "{last_user_message}"
```

To best respond to your **last** message, I invoke this function from the predefined list:
```json
{
    "user_intent": "{{gen 'intent' stop='"' temperature='0.5' max_new_tokens='20' }}",
    "function_name": "{{gen 'function_name' stop='"' top_k='1' }}",
    "invocation": "{{gen 'function_call' stop='"' top_p='0.20' }}"
}
```